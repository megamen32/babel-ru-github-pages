(() => {
  const app = window.BabelApp;
  const { ALG, QUOTE } = app.config;
  const { esc } = app.utils;
  const { sectionShell } = app.views.common;

  app.views.about = function renderAbout() {
    return sectionShell(`
      <section class="about-layout">
        <article class="card about-hero">
          <p class="eyebrow">Алгоритм и доказательство</p>
          <h1>Почему эта библиотека работает как статическое пространство, а не как база данных с готовыми страницами.</h1>
          <blockquote class="inline-quote">
            <p>${esc(QUOTE.text)}</p>
            <footer>${esc(QUOTE.author)}</footer>
          </blockquote>
        </article>
        <article class="card prose-card">
          <h2>1. Что хранится на самом деле</h2>
          <p>Никакие страницы не лежат на диске как отдельные файлы. Хранится только алфавит, геометрия библиотеки и обратимые преобразования между текстом, большим числом и координатами.</p>
          <h2>2. Как текст становится адресом</h2>
          <p>Страница нормализуется под фиксированную длину в ${ALG.pageLength} символов. После этого каждый символ читается как цифра в системе счисления по алфавиту библиотеки. Так получается одно большое число.</p>
          <h2>3. Почему координаты обратимы</h2>
          <p>Число страницы переводится в последовательные координаты: лист, том, полка, стена, зал, сектор. Обратимая перестановка разрывает соседство похожих текстов, но не ломает геометрию адреса. Поэтому каждая страница вычисляется и возвращается без потерь.</p>
          <h2>4. Почему доказательство здесь человеческое</h2>
          <p>Если есть фиксированный алфавит, фиксированная длина страницы и взаимно обратимые функции <code>текст → число → координаты</code> и <code>координаты → число → текст</code>, то любая допустимая страница существует в библиотеке как адрес. Значит, библиотека не хранит всё заранее, но всё равно содержит всё потенциально достижимое пространство страниц.</p>
        </article>
        <article class="card prose-card">
          <h2>Параметры текущей версии</h2>
          <ul class="spec-list">
            <li>Алфавит: ${ALG.alphabet.length} символов.</li>
            <li>Длина страницы: ${ALG.pageLength} символов.</li>
            <li>Страниц в томе: ${ALG.pagesPerVolume}.</li>
            <li>Томов на полке: ${ALG.volumesPerShelf}.</li>
            <li>Полок на стене: ${ALG.shelvesPerWall}.</li>
            <li>Стен в зале: ${ALG.wallsPerHall}.</li>
            <li>Залов в секторе: ${ALG.hallsPerSector}.</li>
          </ul>
        </article>
      </section>
    `);
  };
})();
